[package]
name = "soma-kernel"
version = "0.4.0"
edition = "2021"

[dependencies]
# No external crates permitted per §1 principles

[profile.release]
panic = "abort"
opt-level = "z"     # Optimize for size
lto = true          # Link Time Optimization
codegen-units = 1
